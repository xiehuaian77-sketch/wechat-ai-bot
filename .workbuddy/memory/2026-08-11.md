# 2026-08-11 工作日志

## 完成的工作

### 1. 修复数据库测试初始化问题
- **问题**: API 集成测试报错 `sqlite3.OperationalError: no such table: users`
- **原因**: `tests/conftest.py` 创建了独立的 `engine` 来建表，但 app 实际使用的是 `app.database.session` 模块级 `engine`，两者指向不同的内存数据库
- **修复**: `tests/conftest.py` 改用 `app.database.session.engine` 进行 `Base.metadata.create_all`
- **结果**: 21 个测试全部通过

### 2. 开源准备工作 - 已完成
- ✅ `CHANGELOG.md` - Keep a Changelog 格式，含 [0.1.0] 版本条目
- ✅ `CODE_OF_CONDUCT.md` - Contributor Covenant v2.1
- ✅ `CONTRIBUTING.md` - 5 分钟快速开始、开发规范、提交约定
- ✅ `README.en.md` - 完整英文 README
- ✅ `.github/workflows/ci.yml` - 4 阶段 CI（lint/test/security-audit/dependency-review/build）
- ✅ `.github/workflows/release.yml` - semantic-release 自动化
- ✅ `.releaserc.yml` - conventionalcommits 预设
- ✅ Sentry 集成（`config/settings.py` + `app/main.py`）
- ✅ `SECURITY.md` - 安全策略与漏洞报告流程
- ✅ 依赖安装：`sentry-sdk`

### 3. 代码修复
- `app/middleware/rate_limit.py`: 添加缺失的 `from fastapi.responses import JSONResponse`
- `app/tools/manager.py`: 添加缺失的 `from app.tools.base import ToolResult`
- `tests/conftest.py`: 修复 httpx AsyncClient 兼容性，改用 `ASGITransport`
- `tests/unit/test_middleware.py`: 断言改为子串匹配（IP limiter 与全局 limiter 消息不同）
- `tests/unit/test_tools.py`: 修复 mock `FakeClient.get` 签名，添加默认 `timeout` 参数
- `tests/unit/test_api.py`: 移除重复本地 fixtures，使用共享 conftest

### 4. 测试覆盖率（当前）
- `tests/test_health.py`: 2 passed
- `tests/unit/test_api.py`: 6 passed
- `tests/unit/test_knowledge.py`: 2 passed
- `tests/unit/test_middleware.py`: 2 passed
- `tests/unit/test_tools.py`: 9 passed
- **合计: 21 passed**

## 待办
- [ ] 创建 `SECURITY_AUDIT.md`（P3）
- [ ] 制作示例/教程视频或 GIF（P2）
- [ ] 配置 GitHub Secrets：`SENTRY_DSN`, `CODECOV_TOKEN`, `NPM_TOKEN`
- [ ] 修复 FastAPI `on_event` 弃用警告（改用 lifespan）
